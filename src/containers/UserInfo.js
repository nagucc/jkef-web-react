import React from 'react';
import { connect } from 'react-redux'
import UserInfo from '../../components/Ace/NavBar/UserInfo';

const mapStateToProps = (state, ownProps) => {
	return {
	}
}
export default connect(mapStateToProps)(Index);
